# ─── Server ─────────────────────────────────────────────
# Port the server listens on (default: 3080)
PORT=3080

# ─── Security ──────────────────────────────────────────
# API key for protecting all /api/* routes. When set, every request
# must include an X-API-Key header (or ?api_key= query param).
# Leave empty for trusted LAN / development mode.
PREVUE_API_KEY=

# Encryption key for stored Jellyfin tokens (must be >=32 characters).
# If not set a machine-derived fallback is used (not recommended for production).
DATA_ENCRYPTION_KEY=

# Set to "true" when running behind a reverse proxy (nginx, Caddy, Traefik)
# so the app sees the correct client IP and scheme.
TRUST_PROXY=

# Comma-separated list of allowed CORS origins.
# Leave empty to allow all origins (suitable for LAN / single-host).
# Example: http://localhost:5173,https://prevue.example.com
ALLOWED_ORIGINS=

# Allow connections to private/local-network Jellyfin servers.
# Default behavior is allowed. Set to "0" / "false" / "no" / "off"
# to require public URLs only.
PREVUE_ALLOW_PRIVATE_URLS=1

# ─── Jellyfin ──────────────────────────────────────────
# Jellyfin is configured at runtime via the UI (Settings > Servers).
# No env vars needed for Jellyfin connection.

# ─── Schedule ──────────────────────────────────────────
# Schedule block duration in hours (default: 8)
SCHEDULE_BLOCK_HOURS=8

# ─── Optional integrations ────────────────────────────
# OpenRouter API Key (enables AI channel creation)
OPENROUTER_API_KEY=
